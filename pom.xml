<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.locationtech.geowave</groupId>
	<artifactId>geowave-parent</artifactId>
	<version>3.0.0-SNAPSHOT</version>
	<name>GeoWave Parent POM</name>
	<packaging>pom</packaging>
	<description>GeoWave adds spatio-temporal indexing to keyvalue stores through geotools and
        geoserver
    </description>
	<inceptionYear>2013</inceptionYear>
	<url>https://github.com/locationtech/geowave</url>
	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>
	<!-- Releases go through central-publishing-maven-plugin in the release profile,
		which intercepts deploy and needs no repository entry of its own. -->
	<distributionManagement>
		<snapshotRepository>
			<id>central</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
		</snapshotRepository>
	</distributionManagement>
	<scm>
		<url>https://github.com/locationtech/geowave.git</url>
		<connection>scm:git:https://github.com/locationtech/geowave.git</connection>
		<tag>HEAD</tag>
	</scm>
	<issueManagement>
		<system>GitHub</system>
		<url>https://github.com/locationtech/geowave/issues/</url>
	</issueManagement>
	<mailingLists>
		<mailingList>
			<name>Developer List</name>
			<subscribe>geowave-dev-join@eclipse.org</subscribe>
			<unsubscribe>https://dev.eclipse.org/mailman/listinfo/geowave-dev</unsubscribe>
			<post>geowave-dev@eclipse.org</post>
			<archive>http://eclipse.org/mhonarc/lists/geowave-dev</archive>
		</mailingList>
	</mailingLists>
	<developers>
		<developer>
			<id>rfecher</id>
			<name>Rich Fecher</name>
			<email>rfecher@gmail.com</email>
			<roles>
				<role>developer</role>
				<role>architect</role>
			</roles>
		</developer>
	</developers>
	<properties>
		<geowave-dev-resources.version>1.7</geowave-dev-resources.version>
		<central.publishing.version>0.11.0</central.publishing.version>
		<spark.version>4.0.1</spark.version>
		<uzaygezen.version>0.2</uzaygezen.version>
		<geotools.version>25.0</geotools.version>
		<jts.version>1.18.1</jts.version>
		<imageio.ext.version>1.3.5</imageio.ext.version>
		<guava.version>30.1-jre</guava.version>
		<geoserver.version>2.19.0</geoserver.version>
		<accumulo.version>2.0.1</accumulo.version>
		<thrift.version>0.12.0</thrift.version>
		<cassandra.version>4.0.1</cassandra.version>
		<redisson.version>3.15.5</redisson.version>
		<rocksdb.version>6.19.3</rocksdb.version>
		<cassandraclient.version>4.11.1</cassandraclient.version>
		<cassandraunit.version>4.3.1.0</cassandraunit.version>
		<grpc.version>1.38.0</grpc.version>
		<netty.version>4.1.65.Final</netty.version>
		<protobuf.version>3.17.1</protobuf.version>
		<mavenprotoc.version>3.11.4</mavenprotoc.version>
		<skipITs>true</skipITs>
		<vfs2.version>2.3</vfs2.version>
		<configuration2.version>2.3</configuration2.version>
		<hbase.version>2.4.2</hbase.version>
		<hadoop.version>3.1.2</hadoop.version>
		<avro.version>1.11.4</avro.version>
		<extra.enforcer.rules.version>1.10.0</extra.enforcer.rules.version>
		<maven.compiler.target>21</maven.compiler.target>
		<!-- 1.11.x calls PropertyNamingStrategy.PASCAL_CASE_TO_CAMEL_CASE, which Jackson
			removed in 2.12, so EC2MetadataUtils fails its class initialiser against the
			Jackson that Spark 4 requires. 1.12.788 is the last v1 release and uses the
			current API. v1 is out of support; Phase 2.10 replaces it with v2. -->
		<awssdk.version>1.12.788</awssdk.version>
		<httpclient.version>4.5.13</httpclient.version>
		<httpcore.version>4.4.4</httpcore.version>
		<geotools.scope>compile</geotools.scope>
		<accumulo.scope>compile</accumulo.scope>
		<hbase.scope>compile</hbase.scope>
		<hadoop.scope>compile</hadoop.scope>
		<jetty.version>9.4.43.v20210629</jetty.version>
		<bouncycastle.version>1.69</bouncycastle.version>
		<lz4.version>1.7.1</lz4.version>
		<zookeeper.version>3.5.7</zookeeper.version>
		<jersey.version>2.23.1</jersey.version>
		<scala.version>2.13.16</scala.version>
		<kafka.artifact>kafka_2.13</kafka.artifact>
		<kafka.version>2.8.0</kafka.version>
		<snappy.version>1.1.8.2</snappy.version>
		<collections4.version>4.4</collections4.version>
		<io.version>2.8.0</io.version>
		<text.version>1.13.0</text.version>
		<lang3.version>3.17.0</lang3.version>
		<!-- The generator and the runtime have to move together: a lexer generated by
			4.7 serialises its ATN at version 3 and a 4.13 runtime refuses to read it, and
			the reverse is what broke Spark's own SqlBaseLexer when GeoWave's older runtime
			won on the classpath. -->
		<antlr.version>4.13.1</antlr.version>
		<jcommander.version>1.82</jcommander.version>
		<hbaseprotobuf.version>3.5.1</hbaseprotobuf.version>
		<hbaseprotoc.version>2.5.0</hbaseprotoc.version>
		<hbaseguava.version>12.0.1</hbaseguava.version>
		<jackson.version>1.9.13</jackson.version>
		<beanutils.version>1.11.0</beanutils.version>
		<mockito.version>4.11.0</mockito.version>
		<fasterxml.jackson.version>2.18.2</fasterxml.jackson.version>
		<fasterxml.jackson.databind.version>2.18.2</fasterxml.jackson.databind.version>
		<spring-security.version>5.1.13.RELEASE</spring-security.version>
		<slf4j.version>2.0.16</slf4j.version>
		<log4j.version>2.24.3</log4j.version>
		<jna.version>5.7.0</jna.version>
		<math3.version>3.6.1</math3.version>
		<junit.version>4.13.2</junit.version>
		<clearspring.version>2.9.6</clearspring.version>
		<py4j.version>0.10.9.9</py4j.version>
		<glassfish.el.version>3.0.1-b08</glassfish.el.version>
		<paranamer.version>2.8</paranamer.version>
		<gdal.dir>${project.build.directory}/temp/gdal</gdal.dir>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<sonar.language>java</sonar.language>
		<container.extension />
		<!-- Feed Sonar with the JaCoCo integration tests coverage report (that 
			you have previously generated) -->
		<jacoco.it.reportPath>${project.build.directory}/coverage-reports/jacoco-it.exec
		</jacoco.it.reportPath>
		<jacoco.ut.reportPath>${project.build.directory}/coverage-reports/jacoco-ut.exec
		</jacoco.ut.reportPath>
		<it.reportPath>${project.reporting.outputDirectory}/jacoco-it</it.reportPath>
		<ut.reportPath>${project.reporting.outputDirectory}/jacoco-ut</ut.reportPath>
		<sonar.jacoco.reportPath>${jacoco.ut.reportPath}</sonar.jacoco.reportPath>
		<sonar.jacoco.itReportPath>${jacoco.it.reportPath}</sonar.jacoco.itReportPath>
		<surefire.report.path>${basedir}/target/surefire-reports</surefire.report.path>
		<sonar.junit.reportsPath>${surefire.report.path}</sonar.junit.reportsPath>
		<dash.version>1.1.0</dash.version>
		<jacoco.version>0.8.15</jacoco.version>
		<sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
		<sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
		<!-- The add-opens below are Spark's, not GeoWave's. Spark 4 injects exactly this
			set into every driver and executor it launches, from
			org.apache.spark.launcher.JavaModuleOptions; without them Kryo cannot build a
			serializer for java.nio.HeapByteBuffer, which Spark itself registers, and
			KryoSerializer.newKryo throws before any test runs. The integration suite hosts
			Spark in the test JVM rather than through spark-submit, so nothing else adds
			them. GeoWave's own code needs none of them; that is what the flip removed.
			Spark's add-modules for jdk.incubator.vector is deliberately not carried over:
			Spark treats it as an optimisation and it only produces an incubator warning
			here. -->
		<test.args>-XX:CompressedClassSpaceSize=512m -XX:+UseSerialGC -Xms1g
			-Xmx2g -XX:MaxMetaspaceSize=512m
			--add-opens=java.base/java.lang=ALL-UNNAMED
			--add-opens=java.base/java.lang.invoke=ALL-UNNAMED
			--add-opens=java.base/java.lang.reflect=ALL-UNNAMED
			--add-opens=java.base/java.io=ALL-UNNAMED
			--add-opens=java.base/java.net=ALL-UNNAMED
			--add-opens=java.base/java.nio=ALL-UNNAMED
			--add-opens=java.base/java.util=ALL-UNNAMED
			--add-opens=java.base/java.util.concurrent=ALL-UNNAMED
			--add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED
			--add-opens=java.base/jdk.internal.ref=ALL-UNNAMED
			--add-opens=java.base/sun.nio.ch=ALL-UNNAMED
			--add-opens=java.base/sun.nio.cs=ALL-UNNAMED
			--add-opens=java.base/sun.security.action=ALL-UNNAMED
			--add-opens=java.base/sun.util.calendar=ALL-UNNAMED
			--add-opens=java.security.jgss/sun.security.krb5=ALL-UNNAMED</test.args>
		<spotbugs.version>4.8.6</spotbugs.version>
		<!-- 4.8.6.x is the last line built for Java 8; 4.10.x is class-file major 55
			and would not load on the JDK 8 gate. The plugin and the analysis engine
			version independently, hence two properties. -->
		<spotbugs.plugin.version>4.8.6.6</spotbugs.plugin.version>
		<!-- DoInsideDoPrivileged reports DP_CREATE_CLASSLOADER_INSIDE_DO_PRIVILEGED
			and DP_DO_INSIDE_DO_PRIVILEGED, both of which exist to protect a
			SecurityManager. JEP 411 deprecated that and JEP 486 removed it, so
			wrapping a classloader constructor in AccessController.doPrivileged now
			buys nothing and AccessController itself is deprecated for removal.
			The shared exclude filter lives in the published geowave-dev-resources
			jar and cannot be changed without releasing a new one, so the detector
			is omitted here instead. -->
		<!-- Beyond the two the project already omitted and DoInsideDoPrivileged, these
			are detectors SpotBugs 4 added that do not fit a library of this shape.
			FindPublicAttributes wants public mutable fields made private, which is an
			API break for published types; ConstructorThrow guards against finalizer
			attacks; FindInstanceLockOnSharedStaticData, MultipleInstantiationsOfSingletons
			and FindOverridableMethodCall are hardening rules aimed at code running
			untrusted callers. Together they account for 192 of the 233 findings 4.8.6
			reports. Everything else stays on, including the null-dereference and
			DontCatchNullPointerException detectors, which find real defects here. -->
		<spotbugs.omitVisitors>MutableStaticFields,FindReturnRef,DoInsideDoPrivileged,FindPublicAttributes,ConstructorThrow,FindInstanceLockOnSharedStaticData,MultipleInstantiationsOfSingletons,FindOverridableMethodCall</spotbugs.omitVisitors>
		<spotbugs.effort>Max</spotbugs.effort>
		<spotbugs.threshold>Medium</spotbugs.threshold>
		<spotbugs.failOnError>true</spotbugs.failOnError>
		<spotbugs.maxHeap>1024</spotbugs.maxHeap>
		<!-- A file in this repository rather than the one inside the published
			geowave-dev-resources jar, so a suppression can be added with its reason
			without cutting a dev-resources release. maven.multiModuleProjectDirectory
			is needed because a relative path would resolve per module. -->
		<spotbugs.filterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</spotbugs.filterFile>
		<formatter.action>format</formatter.action>
		<formatter.skip>false</formatter.skip>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
    			<groupId>org.apache.commons</groupId>
    			<artifactId>commons-configuration2</artifactId>
    			<version>${configuration2.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-server</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-servlet</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-xml</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-util</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-security</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-http</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-util</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-webapp</artifactId>
				<version>${jetty.version}</version>
			</dependency>
			<dependency>
				<groupId>net.java.dev.jna</groupId>
				<artifactId>jna</artifactId>
				<version>${jna.version}</version>
			</dependency>
			<dependency>
				<groupId>net.java.dev.jna</groupId>
				<artifactId>jna-platform</artifactId>
				<version>${jna.version}</version>
			</dependency>
			<dependency>
				<groupId>org.scala-lang</groupId>
				<artifactId>scala-library</artifactId>
				<version>${scala.version}</version>
			</dependency>
			<dependency>
				<groupId>org.scala-lang</groupId>
				<artifactId>scala-compiler</artifactId>
				<version>${scala.version}</version>
			</dependency>
			<dependency>
				<groupId>org.scala-lang</groupId>
				<artifactId>jline</artifactId>
				<version>2.11.0-M3</version>
			</dependency>
			<dependency>
				<groupId>org.scala-lang</groupId>
				<artifactId>scala-reflect</artifactId>
				<version>${scala.version}</version>
			</dependency>
			<dependency>
				<groupId>org.scala-lang.modules</groupId>
				<artifactId>scala-collection-compat_2.13</artifactId>
				<version>2.3.0</version>
			</dependency>
			<dependency>
				<groupId>org.locationtech.geowave</groupId>
				<artifactId>geowave-analytic-api</artifactId>
				<version>${project.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.zookeeper</groupId>
				<artifactId>zookeeper</artifactId>
				<version>${zookeeper.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.locationtech.geowave</groupId>
				<artifactId>geowave-datastore-accumulo</artifactId>
				<version>${project.version}</version>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<!-- Spark dependency -->
				<groupId>org.apache.spark</groupId>
				<artifactId>spark-core_2.13</artifactId>
				<version>${spark.version}</version>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<!-- Spark's web UI runs on Jersey 3 and jakarta.ws.rs; GeoWave's services
						are still Jersey 2 and javax.ws.rs, and the two cannot share a JVM.
						Managing jersey.version only pulls the artifacts GeoWave names down to
						2.23.1 and leaves the rest of Spark's tree at 3.x, which is worse than
						either: the services ITs died in RuntimeDelegateImpl with a null HK2
						DynamicConfigurationService. Excluding the UI's stack keeps one coherent
						Jersey on the classpath. Nothing in GeoWave drives Spark's UI, and the
						test session turns it off; this is what Phase 4.5 exists to undo. -->
					<exclusion>
						<groupId>org.glassfish.jersey.core</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.containers</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.inject</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2.external</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<!-- Spark's copy carries the hadoop2 classifier, which is a different
						conflict key from the one GeoWave declares, so Maven mediates neither
						away and both land on the classpath with the same class names. -->
					<exclusion>
						<groupId>org.apache.avro</groupId>
						<artifactId>avro-mapred</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.spark</groupId>
				<artifactId>spark-sql_2.13</artifactId>
				<version>${spark.version}</version>
				<!-- Same Jersey and HK2 story as spark-core: see the note there. -->
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<!-- Spark's web UI runs on Jersey 3 and jakarta.ws.rs; GeoWave's services
						are still Jersey 2 and javax.ws.rs, and the two cannot share a JVM.
						Managing jersey.version only pulls the artifacts GeoWave names down to
						2.23.1 and leaves the rest of Spark's tree at 3.x, which is worse than
						either: the services ITs died in RuntimeDelegateImpl with a null HK2
						DynamicConfigurationService. Excluding the UI's stack keeps one coherent
						Jersey on the classpath. Nothing in GeoWave drives Spark's UI, and the
						test session turns it off; this is what Phase 4.5 exists to undo. -->
					<exclusion>
						<groupId>org.glassfish.jersey.core</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.containers</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.inject</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2.external</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<!-- Spark's copy carries the hadoop2 classifier, which is a different
						conflict key from the one GeoWave declares, so Maven mediates neither
						away and both land on the classpath with the same class names. -->
					<exclusion>
						<groupId>org.apache.avro</groupId>
						<artifactId>avro-mapred</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.spark</groupId>
				<artifactId>spark-mllib_2.13</artifactId>
				<version>${spark.version}</version>
				<!-- mllib pulls spark-sql and spark-streaming, which is the path Jersey 3
					actually arrived by; excluding it on spark-core alone was not enough. -->
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<!-- Spark's web UI runs on Jersey 3 and jakarta.ws.rs; GeoWave's services
						are still Jersey 2 and javax.ws.rs, and the two cannot share a JVM.
						Managing jersey.version only pulls the artifacts GeoWave names down to
						2.23.1 and leaves the rest of Spark's tree at 3.x, which is worse than
						either: the services ITs died in RuntimeDelegateImpl with a null HK2
						DynamicConfigurationService. Excluding the UI's stack keeps one coherent
						Jersey on the classpath. Nothing in GeoWave drives Spark's UI, and the
						test session turns it off; this is what Phase 4.5 exists to undo. -->
					<exclusion>
						<groupId>org.glassfish.jersey.core</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.containers</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.jersey.inject</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.glassfish.hk2.external</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<!-- Spark's copy carries the hadoop2 classifier, which is a different
						conflict key from the one GeoWave declares, so Maven mediates neither
						away and both land on the classpath with the same class names. -->
					<exclusion>
						<groupId>org.apache.avro</groupId>
						<artifactId>avro-mapred</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.locationtech.geowave</groupId>
				<artifactId>geowave-analytic-spark</artifactId>
				<version>${project.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>com.google.uzaygezen</groupId>
				<artifactId>uzaygezen-core</artifactId>
				<version>${uzaygezen.version}</version>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>commons-beanutils</groupId>
				<artifactId>commons-beanutils</artifactId>
				<version>${beanutils.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.thrift</groupId>
				<artifactId>libthrift</artifactId>
				<version>${thrift.version}</version>
			</dependency>
			<dependency>
				<groupId>com.clearspring.analytics</groupId>
				<artifactId>stream</artifactId>
				<version>${clearspring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.bouncycastle</groupId>
				<artifactId>bcprov-jdk15on</artifactId>
				<version>${bouncycastle.version}</version>
			</dependency>
			<dependency>
				<groupId>xerces</groupId>
				<artifactId>xercesImpl</artifactId>
				<version>2.12.1</version>
			</dependency>
			<dependency>
				<groupId>xml-apis</groupId>
				<artifactId>xml-apis</artifactId>
				<version>1.4.01</version>
			</dependency>
			<dependency>
				<groupId>javax.media</groupId>
				<artifactId>jai_codec</artifactId>
				<version>1.1.3</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>javax.media</groupId>
				<artifactId>jai_core</artifactId>
				<version>1.1.3</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>javax.media</groupId>
				<artifactId>jai_imageio</artifactId>
				<version>1.1</version>
			</dependency>
			<dependency>
				<groupId>com.thoughtworks.paranamer</groupId>
				<artifactId>paranamer</artifactId>
				<version>${paranamer.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.ben-manes.caffeine</groupId>
				<artifactId>caffeine</artifactId>
				<version>2.6.2</version>
			</dependency>
			<dependency>
				<groupId>org.locationtech.jts</groupId>
				<artifactId>jts-core</artifactId>
				<version>${jts.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-1.2-api</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<!-- https://mvnrepository.com/artifact/org.apache.logging.log4j/log4j-slf4j2-impl -->
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-slf4j2-impl</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.logging.log4j</groupId>
				<artifactId>log4j-core</artifactId>
				<version>${log4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-pool2</artifactId>
				<version>2.4.2</version>
			</dependency>
			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${io.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>${lang3.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-collections4</artifactId>
				<version>${collections4.version}</version>
			</dependency>
			<dependency>
				<groupId>commons-codec</groupId>
				<artifactId>commons-codec</artifactId>
				<version>1.9</version>
			</dependency>
			<dependency>
				<groupId>com.google.guava</groupId>
				<artifactId>guava</artifactId>
				<version>${guava.version}</version>
			</dependency>
			<dependency>
				<groupId>com.oath.cyclops</groupId>
				<artifactId>cyclops</artifactId>
				<version>10.4.0</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>jcl-over-slf4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-math3</artifactId>
				<version>${math3.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-text</artifactId>
				<version>${text.version}</version>
			</dependency>
			<dependency>
				<groupId>org.threeten</groupId>
				<artifactId>threeten-extra</artifactId>
				<version>1.4</version>
			</dependency>
			<dependency>
				<groupId>org.xerial.snappy</groupId>
				<artifactId>snappy-java</artifactId>
				<version>${snappy.version}</version>
			</dependency>
			<dependency>
				<groupId>org.redisson</groupId>
				<artifactId>redisson</artifactId>
				<version>${redisson.version}</version>
			</dependency>
			<dependency>
				<groupId>org.rocksdb</groupId>
				<artifactId>rocksdbjni</artifactId>
				<version>${rocksdb.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-core</artifactId>
				<version>${accumulo.version}</version>
				<scope>${accumulo.scope}</scope>
				<exclusions>
					<!-- hibernate-validator 6.0.11 activates a <jdk>[11,)</jdk> profile that
						depends on org.openjfx:javafx.base:11.0.0-SNAPSHOT, which does not
						exist. Harmless on JDK 8, fatal to resolution on anything newer. -->
					<exclusion>
						<groupId>org.openjfx</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.locationtech.geowave</groupId>
				<artifactId>geowave-cli-accumulo-embed</artifactId>
				<version>${project.version}</version>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-server</artifactId>
				<version>${accumulo.version}</version>
				<scope>${accumulo.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-monitor</artifactId>
				<version>${accumulo.version}</version>
				<scope>${accumulo.scope}</scope>
				<exclusions>
					<!-- hibernate-validator 6.0.11 activates a <jdk>[11,)</jdk> profile that
						depends on org.openjfx:javafx.base:11.0.0-SNAPSHOT, which does not
						exist. Harmless on JDK 8, fatal to resolution on anything newer. -->
					<exclusion>
						<groupId>org.openjfx</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-test</artifactId>
				<version>${accumulo.version}</version>
				<exclusions>
					<!-- hibernate-validator 6.0.11 activates a <jdk>[11,)</jdk> profile that
						depends on org.openjfx:javafx.base:11.0.0-SNAPSHOT, which does not
						exist. Harmless on JDK 8, fatal to resolution on anything newer. -->
					<exclusion>
						<groupId>org.openjfx</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-minicluster</artifactId>
				<version>${accumulo.version}</version>
				<exclusions>
					<!-- hibernate-validator 6.0.11 activates a <jdk>[11,)</jdk> profile that
						depends on org.openjfx:javafx.base:11.0.0-SNAPSHOT, which does not
						exist. Harmless on JDK 8, fatal to resolution on anything newer. -->
					<exclusion>
						<groupId>org.openjfx</groupId>
						<artifactId>*</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.accumulo</groupId>
				<artifactId>accumulo-shell</artifactId>
				<version>${accumulo.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-testing-util</artifactId>
				<version>${hbase.version}</version>
				<exclusions>
					<exclusion>
						<artifactId>jdk.tools</artifactId>
						<groupId>jdk.tools</groupId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-shaded-client</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-client</artifactId>
				<version>${hbase.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-server</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-server</artifactId>
				<version>${hbase.version}</version>
				<type>test-jar</type>
				<scope>${hbase.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-common</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-common</artifactId>
				<version>${hbase.version}</version>
				<type>test-jar</type>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-protocol</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-annotations</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-annotations</artifactId>
				<version>${hbase.version}</version>
				<type>test-jar</type>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-hadoop-compat</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-hadoop-compat</artifactId>
				<version>${hbase.version}</version>
				<type>test-jar</type>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-hadoop2-compat</artifactId>
				<version>${hbase.version}</version>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hbase</groupId>
				<artifactId>hbase-hadoop2-compat</artifactId>
				<version>${hbase.version}</version>
				<type>test-jar</type>
				<scope>${hbase.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-client</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-minikdc</artifactId>
				<version>${hadoop.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-annotations</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-distcp</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-common</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-auth</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-hdfs</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-hdfs</artifactId>
				<version>${hadoop.version}</version>
				<type>test-jar</type>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-minicluster</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
					<exclusion>
						<groupId>log4j</groupId>
						<artifactId>log4j</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-mapreduce-client-core</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.hadoop</groupId>
				<artifactId>hadoop-mapreduce-client-jobclient</artifactId>
				<version>${hadoop.version}</version>
				<scope>${hadoop.scope}</scope>
			</dependency>
			<dependency>
				<groupId>com.beust</groupId>
				<artifactId>jcommander</artifactId>
				<version>${jcommander.version}</version>
			</dependency>
			<dependency>
				<groupId>it.geosolutions.imageio-ext</groupId>
				<artifactId>imageio-ext-gdalgeotiff</artifactId>
				<version>${imageio.ext.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-imageio-ext-gdal</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
					<exclusion>
						<artifactId>gt-epsg-hsql</artifactId>
						<groupId>org.geotools</groupId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-referencing</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-api</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-cql</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-geojson</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-geojsondatastore</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-opengis</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-shapefile</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-main</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-data</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-wps</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-netcdf</artifactId>
				<version>${geotools.version}</version>
				<exclusions>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.hsqldb</groupId>
				<artifactId>hsqldb</artifactId>
				<version>2.3.0</version>
			</dependency>
			<dependency>
				<groupId>org.geoserver</groupId>
				<artifactId>gs-wms</artifactId>
				<version>${geoserver.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.geoserver</groupId>
				<artifactId>gs-main</artifactId>
				<version>${geoserver.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.geoserver</groupId>
				<artifactId>gs-ows</artifactId>
				<version>${geoserver.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geoserver</groupId>
				<artifactId>gs-platform</artifactId>
				<version>${geoserver.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geoserver</groupId>
				<artifactId>gs-wfs</artifactId>
				<version>${geoserver.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-render</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
				<exclusions>
					<exclusion>
						<groupId>com.vividsolutions</groupId>
						<artifactId>jts</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.cassandraunit</groupId>
				<artifactId>cassandra-unit</artifactId>
				<version>${cassandraunit.version}</version>
			</dependency>
			<dependency>
				<groupId>org.geotools</groupId>
				<artifactId>gt-epsg-wkt</artifactId>
				<version>${geotools.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.avro</groupId>
				<artifactId>avro</artifactId>
				<version>${avro.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.avro</groupId>
				<artifactId>avro-mapred</artifactId>
				<version>${avro.version}</version>
			</dependency>
			<dependency>
				<groupId>com.amazonaws</groupId>
				<artifactId>aws-java-sdk-s3</artifactId>
				<version>${awssdk.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.httpcomponents</groupId>
				<artifactId>httpclient</artifactId>
				<version>${httpclient.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.httpcomponents</groupId>
				<artifactId>httpcore</artifactId>
				<version>${httpcore.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs</artifactId>
				<version>${spotbugs.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-annotations</artifactId>
				<version>${spotbugs.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.media</groupId>
				<artifactId>jersey-media-multipart</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.core</groupId>
				<artifactId>jersey-client</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.core</groupId>
				<artifactId>jersey-common</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.core</groupId>
				<artifactId>jersey-server</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.containers</groupId>
				<artifactId>jersey-container-servlet</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.media</groupId>
				<artifactId>jersey-media-json-jackson</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jersey.ext</groupId>
				<artifactId>jersey-proxy-client</artifactId>
				<version>${jersey.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-vfs2</artifactId>
				<version>${vfs2.version}</version>
			</dependency>
			<dependency>
				<groupId>org.codehaus.jackson</groupId>
				<artifactId>jackson-mapper-asl</artifactId>
				<version>${jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>org.codehaus.jackson</groupId>
				<artifactId>jackson-core-asl</artifactId>
				<version>${jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-annotations</artifactId>
				<version>${fasterxml.jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-databind</artifactId>
				<version>${fasterxml.jackson.databind.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-core</artifactId>
				<version>${fasterxml.jackson.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.dataformat</groupId>
				<artifactId>jackson-dataformat-cbor</artifactId>
				<version>${fasterxml.jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.dataformat</groupId>
				<artifactId>jackson-dataformat-yaml</artifactId>
				<version>${fasterxml.jackson.version}</version>
			</dependency>
			<!-- Kafka brings jackson-module-scala at its own Jackson version, and the
				module refuses to load against a databind whose minor version differs. It
				says so and then throws, which is fatal to every Spark job, because
				RDDOperationScope registers it before anything else runs. Managing the
				coordinate keeps it with the rest of Jackson. The _2.11 coordinate this
				replaces was a leftover nothing had referred to since Scala 2.11. -->
			<dependency>
				<groupId>com.fasterxml.jackson.module</groupId>
				<artifactId>jackson-module-scala_2.13</artifactId>
				<version>${fasterxml.jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-core</artifactId>
				<version>${spring-security.version}</version>
				<scope>${geotools.scope}</scope>
			</dependency>
			<dependency>
				<groupId>org.lz4</groupId>
				<artifactId>lz4-java</artifactId>
				<version>${lz4.version}</version>
			</dependency>
			<dependency>
				<groupId>com.google.protobuf</groupId>
				<artifactId>protobuf-java</artifactId>
				<version>${protobuf.version}</version>
			</dependency>
			<dependency>
				<groupId>com.google.protobuf</groupId>
				<artifactId>protobuf-java-util</artifactId>
				<version>${protobuf.version}</version>
			</dependency>
			<dependency>
				<groupId>io.grpc</groupId>
				<artifactId>grpc-protobuf</artifactId>
				<version>${grpc.version}</version>
			</dependency>
			<dependency>
				<groupId>io.grpc</groupId>
				<artifactId>grpc-stub</artifactId>
				<version>${grpc.version}</version>
			</dependency>
			<dependency>
				<groupId>io.grpc</groupId>
				<artifactId>grpc-common</artifactId>
				<version>${grpc.version}</version>
			</dependency>
			<dependency>
				<groupId>io.grpc</groupId>
				<artifactId>grpc-protobuf-lite</artifactId>
				<version>${grpc.version}</version>
			</dependency>
			<dependency>
				<groupId>io.grpc</groupId>
				<artifactId>grpc-netty</artifactId>
				<version>${grpc.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-all</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-common</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-codec-http2</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-codec-socks</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-codec</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-buffer</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-transport</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-handler</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-handler-proxy</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-resolver</artifactId>
				<version>${netty.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish</groupId>
				<artifactId>javax.el</artifactId>
				<version>${glassfish.el.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<!-- Dependencies used across ALL projects -->
	<dependencies>
		<!-- https://mvnrepository.com/artifact/org.apache.logging.log4j/log4j-slf4j2-impl -->
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-slf4j2-impl</artifactId>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.apache.logging.log4j/log4j-core -->
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-1.2-api</artifactId>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
		<extensions>
			<extension>
				<groupId>org.springframework.build</groupId>
				<artifactId>aws-maven</artifactId>
				<version>5.0.0.RELEASE</version>
			</extension>
		</extensions>
		<pluginManagement>
			<plugins>
				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>org.jacoco</groupId>
										<artifactId>
											jacoco-maven-plugin
										</artifactId>
										<versionRange>
											[${jacoco.version},)
										</versionRange>
										<goals>
											<goal>prepare-agent</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore />
									</action>
								</pluginExecution>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>org.apache.avro</groupId>
										<artifactId>avro-maven-plugin</artifactId>
										<versionRange>[1.7.6,)</versionRange>
										<goals>
											<goal>schema</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<execute />
									</action>
								</pluginExecution>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>
											net.revelc.code.formatter
										</groupId>
										<artifactId>
											formatter-maven-plugin
										</artifactId>
										<versionRange>
											[0.5.2,)
										</versionRange>
										<goals>
											<goal>format</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore />
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
				<plugin>
					<groupId>com.github.spotbugs</groupId>
					<artifactId>spotbugs-maven-plugin</artifactId>
					<version>${spotbugs.plugin.version}</version>
					<configuration>
						<failOnError>${spotbugs.failOnError}</failOnError>
						<threshold>${spotbugs.threshold}</threshold>
						<effort>${spotbugs.effort}</effort>
						<excludeFilterFile>${spotbugs.filterFile}</excludeFilterFile>
						<maxHeap>${spotbugs.maxHeap}</maxHeap>
						<jvmArgs>${spotbugs.args}</jvmArgs>
						<omitVisitors>${spotbugs.omitVisitors}</omitVisitors>
					</configuration>
					<dependencies>
						<dependency>
							<groupId>org.locationtech.geowave</groupId>
							<artifactId>geowave-dev-resources</artifactId>
							<version>${geowave-dev-resources.version}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>org.apache.avro</groupId>
					<artifactId>avro-maven-plugin</artifactId>
					<version>${avro.version}</version>
					<executions>
						<execution>
							<phase>generate-sources</phase>
							<goals>
								<goal>schema</goal>
							</goals>
							<configuration>
								<sourceDirectory>${project.basedir}/src/main/avro/</sourceDirectory>
								<outputDirectory>${project.basedir}/src/main/java/</outputDirectory>
								<stringType>String</stringType>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>net.revelc.code.formatter</groupId>
					<artifactId>formatter-maven-plugin</artifactId>
					<version>2.11.0</version>
				</plugin>
				<plugin>
					<groupId>org.eclipse.dash</groupId>
					<artifactId>license-tool-plugin</artifactId>
					<version>${dash.version}</version>
					<configuration>
						<!-- Eclipse's concern is what we redistribute, so test content is
							out of scope. -->
						<excludeScope>test</excludeScope>
						<projectId>locationtech.geowave</projectId>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>pre-unit-test</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<includes>
								<include>org/locationtech/geowave/**</include>
							</includes>
							<destFile>${jacoco.ut.reportPath}</destFile>
							<propertyName>jacoco.surefire.argline</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>post-unit-test</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${jacoco.ut.reportPath}</dataFile>
							<outputDirectory>${ut.reportPath}</outputDirectory>
						</configuration>
					</execution>
					<execution>
						<id>pre-integration-test</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<includes>
								<include>org/locationtech/geowave/**</include>
							</includes>
							<destFile>${jacoco.it.reportPath}</destFile>
							<propertyName>jacoco.failsafe.argline</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>post-integration-test</id>
						<phase>post-integration-test</phase>
						<goals>
							<goal>report-integration</goal>
						</goals>
						<configuration>
							<dataFile>${jacoco.it.reportPath}</dataFile>
							<outputDirectory>${it.reportPath}</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>3.6.3</version>
				<executions>
					<execution>
						<id>enforce-build-environment</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<!-- Several plugins are declared without a version and so resolve
									against the super-POM of whichever Maven is running. Requiring a
									floor keeps that resolution the same for everyone. -->
								<requireMavenVersion>
									<version>[3.9.0,)</version>
								</requireMavenVersion>
								<!-- The build targets Java 8 and CI compiles on JDK 8, but a
									dependency bump can quietly bring in a jar built for a later
									release: javac then refuses to read it and the failure names a
									class file version rather than the dependency that caused it.
									Avro 1.12 is exactly this: Java 11 bytecode, and the bump to it
									was reviewed on JDK 17 where nothing complained. Check the
									dependencies against the compiler target instead of finding out
									from javac. -->
								<enforceBytecodeVersion>
									<maxJdkVersion>${maven.compiler.target}</maxJdkVersion>
									<ignoreClasses>
										<!-- Multi-release jars keep newer bytecode under
											META-INF/versions, which a Java 8 runtime never loads. -->
										<ignoreClass>META-INF.versions.*</ignoreClass>
										<ignoreClass>module-info</ignoreClass>
										<!-- The only pre-existing violation in the whole graph: five
											classes in akka-stream that bridge to java.util.concurrent.Flow
											and are compiled for 11 on purpose. Nothing loads them unless it
											asks for JavaFlowSupport, and nothing here does. -->
										<ignoreClass>akka.stream.javadsl.JavaFlowSupport*</ignoreClass>
									</ignoreClasses>
								</enforceBytecodeVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>extra-enforcer-rules</artifactId>
						<version>${extra.enforcer.rules.version}</version>
					</dependency>
				</dependencies>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.16.0</version>
				<configuration>
					<release>${maven.compiler.target}</release>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
				<version>2.5</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.6.0</version>
				<configuration>
					<argLine>${test.args} ${jacoco.surefire.argline}</argLine>
					<excludes>
						<exclude>**/*IT.java</exclude>
						<exclude>**/*ITSuite.java</exclude>
					</excludes>
					<environmentVariables>
						<LD_LIBRARY_PATH>${gdal.dir}</LD_LIBRARY_PATH>
						<PATH>${gdal.dir}${path.separator}${env.PATH}</PATH>
						<GDAL_DIR>${gdal.dir}</GDAL_DIR>
						<NCS_USER_PREFS />
					</environmentVariables>
					<outputDirectory>${surefire.report.path}</outputDirectory>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.12.0</version>
				<configuration>
					<notimestamp>true</notimestamp>
					<additionalparam>${javadoc.opts}</additionalparam>
				</configuration>
				<executions>
					<execution>
						<id>aggregate</id>
						<goals>
							<goal>aggregate</goal>
						</goals>
						<phase>site</phase>
						<configuration>
							<sourceFileIncludes>
								<include>**.java</include>
							</sourceFileIncludes>
							<quiet>true</quiet>
							<windowtitle>GeoWave ${project.version}</windowtitle>
							<doctitle>GeoWave ${project.version}</doctitle>
							<groups>
								<group>
									<title>Analytics</title>
									<packages>
										org.locationtech.geowave.analytic*:org.locationtech.geowave.mapreduce*
									</packages>
								</group>
								<group>
									<title>Data Adapter for Vector Data</title>
									<packages>org.locationtech.geowave.adapter.vector*</packages>
								</group>
								<group>
									<title>Data Adapter for Raster Data</title>
									<packages>org.locationtech.geowave.adapter.raster*</packages>
								</group>
								<group>
									<title>Data Adapter Authorizations</title>
									<packages>org.locationtech.geowave.adapter.auth*</packages>
								</group>
								<group>
									<title>Storage API</title>
									<packages>org.locationtech.geowave.core.store*</packages>
								</group>
								<group>
									<title>Spatial-Temporal Core</title>
									<packages>org.locationtech.geowave.core.geotime*</packages>
								</group>
								<group>
									<title>Accumulo Data Store</title>
									<packages>org.locationtech.geowave.datastore.accumulo*
									</packages>
								</group>
								<group>
									<title>HBase Data Store</title>
									<packages>org.locationtech.geowave.datastore.hbase*</packages>
								</group>
								<group>
									<title>Indexing</title>
									<packages>org.locationtech.geowave.core.index*</packages>
								</group>
								<group>
									<title>Command-Line Tools</title>
									<packages>
										org.locationtech.geowave.core.cli*:org.locationtech.geowave.cli*
									</packages>
								</group>
								<group>
									<title>Ingest Framework</title>
									<packages>org.locationtech.geowave.core.ingest*</packages>
								</group>
								<group>
									<title>Base Ingest Formats</title>
									<packages>org.locationtech.geowave.format*</packages>
								</group>
								<group>
									<title>Services</title>
									<packages>org.locationtech.geowave.service*</packages>
								</group>
								<group>
									<title>Examples</title>
									<packages>org.locationtech.geowave.example*</packages>
								</group>
							</groups>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<!-- for incrementing version numbers and tagging repositories To Use: 
				0. Ensure all files checked into master (clean environment). Make sure you 
				have access to both mvn and git on your command line, and you are in a terminal 
				that has push access (credentials are setup and correct). Make sure you have 
				the branch active in git you want to tag. If something goes wrong pushing, 
				you will most likely have outstanding commits in your local repo you will 
				have to revert/remove. 1. mvn release:prepare -DdryRun=true -Darguments="-DskipTests 
				-Dspotbugs.skip" 2. Inspect and ensure the correct pom.xml files will be 
				modified 3. mvn release:prepare -Darguments="-DskipTests -Dspotbugs.skip" 
				4. mvn release:clean -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>3.3.1</version>
				<configuration>
					<tagNameFormat>v@{project.version}</tagNameFormat>
					<autoVersionSubmodules>true</autoVersionSubmodules>
				</configuration>
			</plugin>
			<plugin>
				<groupId>net.revelc.code.formatter</groupId>
				<artifactId>formatter-maven-plugin</artifactId>
				<configuration>
					<configFile>eclipse/eclipse-formatter.xml</configFile>
					<lineEnding>KEEP</lineEnding>
					<skip>${formatter.skip}</skip>
					<excludes>
						<exclude>**/*Protos*.java</exclude>
						<exclude>**/Avro*.java</exclude>
						<exclude>**/*Grpc.java</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>${formatter.action}</goal>
						</goals>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>org.locationtech.geowave</groupId>
						<artifactId>geowave-dev-resources</artifactId>
						<version>${geowave-dev-resources.version}</version>
					</dependency>
				</dependencies>
			</plugin>

		</plugins>
	</build>
	<modules>
		<module>analytics</module>
		<module>core</module>
		<module>deploy</module>
		<module>docs</module>
		<module>examples/java-api</module>
		<module>extensions</module>
		<module>python</module>
		<module>migration</module>
		<module>services</module>
		<module>test</module>
	</modules>
	<repositories>
		<repository>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<id>osgeo-snapshot</id>
			<name>Open Source Geospatial Foundation Build Repository</name>
			<url>https://repo.osgeo.org/repository/snapshot/</url>
		</repository>
		<repository>
			<id>osgeo-release</id>
			<name>Open Source Geospatial Foundation Repository</name>
			<url>https://repo.osgeo.org/repository/release/</url>
		</repository>
		<repository>
			<id>central-snapshots</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
			<releases>
				<enabled>false</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</repository>
	</repositories>

	<pluginRepositories>
		<!-- The Eclipse Dash license tool is published by the Eclipse Foundation, not to
			Maven Central. -->
		<pluginRepository>
			<id>dash-licenses-releases</id>
			<url>https://repo.eclipse.org/content/repositories/dash-licenses-releases/</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>
	<profiles>
		<profile>
			<!-- spotbugs-maven-plugin 3.1.10 bundles a Groovy that cannot initialize on
				JDK 9 or later: the mojo fails to load before it ever reads its own skip
				parameter, so -Dspotbugs.skip alone cannot avoid it. Binding the execution
				through a profile means the property deactivates the profile and the plugin
				is never resolved at all, which is what -Dspotbugs.skip is expected to do.
				This can go away once the analyzer is upgraded. -->
			<id>spotbugs</id>
			<activation>
				<property>
					<name>!spotbugs.skip</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.github.spotbugs</groupId>
						<artifactId>spotbugs-maven-plugin</artifactId>
						<executions>
							<execution>
								<phase>verify</phase>
								<goals>
									<goal>check</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>build-installer-plugin</id>
			<properties>
				<!-- standalone installer seems to work better at least on windows with 
					hadoop2 (more compatible with local FS), hbase shaded client is packaged 
					with 2.10.0, which is what the ITs end up finding first -->
				<hadoop.version>2.10.0</hadoop.version>
			</properties>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<artifactId>maven-assembly-plugin</artifactId>
							<version>3.1.1</version>
							<configuration>
								<finalName>installer-plugin</finalName>
								<appendAssemblyId>false</appendAssemblyId>
								<descriptorRefs>
									<descriptorRef>default-installer-plugin</descriptorRef>
								</descriptorRefs>
							</configuration>
							<executions>
								<execution>
									<id>make-assembly
                                    </id> <!-- this is used for inheritance merges -->
									<phase>package</phase> <!-- bind to the packaging phase -->
									<goals>
										<goal>single</goal>
									</goals>
								</execution>
							</executions>
							<dependencies>
								<dependency>
									<groupId>org.locationtech.geowave</groupId>
									<artifactId>geowave-dev-resources</artifactId>
									<version>${geowave-dev-resources.version}</version>
								</dependency>
							</dependencies>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
		<!-- skip all code quality tools if specified -->
		<profile>
			<id>skip-code-quality</id>
			<activation>
				<property>
					<name>skip-quality</name>
				</property>
			</activation>
			<properties>
				<skipITs>true</skipITs>
				<skipTests>true</skipTests>
				<formatter.skip>true</formatter.skip>
				<spotbugs.skip>true</spotbugs.skip>
			</properties>
		</profile>
		<profile>
			<id>doclint-java8-disable</id>
			<activation>
				<jdk>[1.8,)</jdk>
			</activation>
			<properties>
				<javadoc.opts>-Xdoclint:none</javadoc.opts>
			</properties>
		</profile>
		<!-- Activate using the release property: mvn clean install -Prelease -->
		<profile>
			<id>release</id>

			<build>
				<plugins>
					<!-- To release to Maven central. OSSRH was retired on 2025-06-30; this
						replaces nexus-staging-maven-plugin, and SONATYPE_USERNAME/SONATYPE_PASSWORD
						must now be a Central Portal user token rather than JIRA credentials. -->
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>${central.publishing.version}</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>central</publishingServerId>
							<autoPublish>true</autoPublish>
							<waitUntilPublished>true</waitUntilPublished>
						</configuration>
					</plugin>
					<!-- To generate javadoc -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-source-plugin</artifactId>
						<version>3.4.0</version>
						<executions>
							<execution>
								<id>attach-sources</id>
								<goals>
									<goal>jar-no-fork</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<version>3.12.0</version>
						<executions>
							<execution>
								<id>attach-javadocs</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
					</plugin>

					<!-- To sign the artifacts -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.8</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
								<configuration>
									<!-- Prevent `gpg` from using pinentry programs -->
									<gpgArguments>
										<arg>--pinentry-mode</arg>
										<arg>loopback</arg>
									</gpgArguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>